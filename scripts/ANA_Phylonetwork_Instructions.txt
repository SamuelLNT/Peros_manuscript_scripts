## Update for the ingroup run, August 7
# dataset is Eros_ingrp_REF_icarus_aftrkr_merg_n52_c85_m42.loci
# I did not apply any filters and just took the loci file.  
# Since you have been using it for other analyses it makes sense to be consistent and skip them.
# alternative would be to re-run everything with every filter combination.
# No data to back it up but missing data is the main filter we use and we already tested the datasets with different taxon coverage AKA missing data filter.

# STEPS

# 1
# I created the map files by updating the previously made for the outgroup
# I edited the files on a cluster
# 2 I split loci file to phy

python3 convert_from0.py -L Eros_REF_icarus_aftrkr_merg_n54_c85_m35.loci

ls loci/ | head
locus0.phy
locus1000.phy
locus1001.phy
locus1002.phy
locus1003.phy
locus1004.phy
locus1005.phy
locus1006.phy
locus1007.phy
locus1008.phy

# 3
# I ran the raxml using the same script as before that can be found in "/scratch/project_number/ddRAD_demultiplexed_projects/004_eros_eroides/process_files/Eros_icarus_REF/PhyloNet/000_Uladz_phylonetworks/loci_phy_conversion/raxml.pl"
# the batch script is here "/scratch/project_number/ddRAD_demultiplexed_projects/004_eros_eroides/process_files/Eros_icarus_REF/PhyloNet/000_Uladz_phylonetworks/loci_phy_conversion/raxml_batch.sh"

# I ran ASTRAL as described in here "/scratch/project_number/ddRAD_demultiplexed_projects/004_eros_eroides/process_files/Eros_icarus_REF/PhyloNet/scripts_metadata/Instructions_Uladz_for_Phylonetworks.txt"

module load biojava
java -jar /scratch/project_number/Programme/astral_program/Astral/astral.5.7.8.jar -i /scratch/project_number/ddRAD_demultiplexed_projects/004_eros_eroides/process_files/Eros_icarus_REF/PhyloNet/002_Uladz_phylonetworks_n54_icarus_outgroup/loci_phy_conversion_raxml/raxml/besttrees.tre -a /scratch/project_number/ddRAD_demultiplexed_projects/004_eros_eroides/process_files/Eros_icarus_REF/PhyloNet/002_Uladz_phylonetworks_n54_icarus_outgroup/eros_MAP_ASTRAL.txt -o Eros_REF_icarus_aftrkr_merg_n54_c85_m35.tre > astral.screen.log 2>&1

# 4
# Phylonetworks
# I followed the same parameters that we had in the previous runs.
sinteractive -m 10000 -c 8 --account project_2002062 --time 04:00:00 --tmp 10
module load julia
julia
using Distributed
addprocs(8)


## it seems the packages are absent, so I have to reinstall them
]  ##Calls package installer, you need to add the packages only once
add RCall
build RCall # seems to be obsolete 
add PhyloPlots 
add PhyloNetworks # was installed when I tried to run using PhyloNetworks
add CSV
add DataFrames
#Press backspace to be back to julia prompt

using PhyloNetworks # after 000 run with total n55 sampling phylonetworks do no require installation
using RCall # already installed
using PhyloPlots # already installed
using CSV # already installed
using DataFrames # already installed



dt = CSV.read("/scratch/project_number/ddRAD_demultiplexed_projects/004_eros_eroides/process_files/Eros_icarus_REF/PhyloNet/002_Uladz_phylonetworks_n54_icarus_outgroup/eros_MAP_PHYLONET.txt",DataFrame)
taxonmap = Dict(dt[i,:specimen] => dt[i,:species] for i in 1:54) # 54 is the number of individuals

raxmltrees = "/scratch/project_number/ddRAD_demultiplexed_projects/004_eros_eroides/process_files/Eros_icarus_REF/PhyloNet/002_Uladz_phylonetworks_n54_icarus_outgroup/loci_phy_conversion_raxml/raxml/besttrees.tre"

genetrees = readMultiTopology(raxmltrees)

df_sp = writeTableCF(countquartetsintrees(genetrees, taxonmap)...) # taxon names should be species names, not allele names here

CSV.write("tableCF.csv", df_sp) # to save the data frame to a file
raxmlCF = readTableCF("tableCF.csv") # read in the file and produces a "DataCF" object

astralfile = "/scratch/project_number/ddRAD_demultiplexed_projects/004_eros_eroides/process_files/Eros_icarus_REF/PhyloNet/002_Uladz_phylonetworks_n54_icarus_outgroup/astral/Eros_REF_icarus_aftrkr_merg_n54_c85_m35.tre" # the generated astrak tree
astraltree = readMultiTopology(astralfile)[1] # read the 1st tree

###can be done without outgroup. possibly just silence this line and consequtive rootatnode lines.
# rootonedge!(astraltree, 9) 

## In eros case could be 
rootatnode!(astraltree, "icar")


R"name = function(x) file.path(x)" # function to create file name in appropriate folder
R"svg(name('astral_tree_root.svg'), width=8, height=12)" # starts image file
R"par"(mar=[0,0,0,0]) # to reduce margins (no margins at all here)
plot(astraltree, :R, showEdgeLength=true, showEdgeNumber=true); # network is plotted & sent to file
R"dev.off()"; # wrap up and save image file

net0 = snaq!(astraltree, raxmlCF, hmax=0, filename="net0", seed=4353, runs=100)
rootatnode!(net0, "icar")

R"name = function(x) file.path(x)" # function to create file name in appropriate folder
R"svg(name('net0.svg'), width=8, height=12)" # starts image file
R"par"(mar=[0,0,0,0]) # to reduce margins (no margins at all here)
plot(net0, :R, showEdgeNumber=true); # network is plotted & sent to file
R"dev.off()"; # wrap up and save image file


net1 = snaq!(net0, raxmlCF, hmax=1, filename="net1", seed=2345, runs=100)
rootatnode!(net1, "icar")

R"name = function(x) file.path(x)" # function to create file name in appropriate folder
R"svg(name('net1.svg'), width=8, height=12)" # starts image file
R"par"(mar=[0,0,0,0]) # to reduce margins (no margins at all here)
plot(net1, :R, showGamma=true, showEdgeNumber=true); # network is plotted & sent to file
R"dev.off()"; # wrap up and save image file

#less("net1.err") # would provide info about errors, if any
#less("net1.out") # main output file with the estimated network from each run
#less("net1.networks") # extra info


net2 = snaq!(net1,raxmlCF, hmax=2, filename="net2", seed=3456, runs=100)
rootatnode!(net2, "icar")

R"name = function(x) file.path(x)" # function to create file name in appropriate folder
R"svg(name('net2.svg'), width=8, height=12)" # starts image file
R"par"(mar=[0,0,0,0]) # to reduce margins (no margins at all here)
plot(net2, :R, showGamma=true, showEdgeNumber=true); # network is plotted & sent to file
R"dev.off()"; # wrap up and save image file

#less("net2.err") # would provide info about errors, if any
#less("net2.out") # main output file with the estimated network from each run
#less("net2.networks") # extra info

net3 = snaq!(net2,raxmlCF, hmax=3, filename="net3", seed=4567, runs=100)
rootatnode!(net3, "icar")

R"name = function(x) file.path(x)" # function to create file name in appropriate folder
R"svg(name('net3.svg'), width=8, height=12)" # starts image file
R"par"(mar=[0,0,0,0]) # to reduce margins (no margins at all here)
plot(net3, :R, showGamma=true, showEdgeNumber=true); # network is plotted & sent to file
R"dev.off()"; # wrap up and save image file

#less("net3.err") # would provide info about errors, if any
#less("net3.out") # main output file with the estimated network from each run
#less("net3.networks") # extra info

net4 = snaq!(net3,raxmlCF, hmax=4, filename="net4", seed=5567, runs=100)
rootatnode!(net4, "icar")

R"name = function(x) file.path(x)" # function to create file name in appropriate folder
R"svg(name('net4.svg'), width=8, height=12)" # starts image file
R"par"(mar=[0,0,0,0]) # to reduce margins (no margins at all here)
plot(net4, :R, showGamma=true, showEdgeNumber=true); # network is plotted & sent to file
R"dev.off()"; # wrap up and save image file

#less("net4.err") # would provide info about errors, if any
#less("net4.out") # main output file with the estimated network from each run
#less("net4.networks") # extra info

net5 = snaq!(net4,raxmlCF, hmax=5, filename="net5", seed=5567, runs=100)
rootatnode!(net5, "icar")

R"name = function(x) file.path(x)" # function to create file name in appropriate folder
R"svg(name('net5.svg'), width=8, height=12)" # starts image file
R"par"(mar=[0,0,0,0]) # to reduce margins (no margins at all here)
plot(net5, :R, showGamma=true, showEdgeNumber=true); # network is plotted & sent to file
R"dev.off()"; # wrap up and save image file

#less("net4.err") # would provide info about errors, if any
#less("net4.out") # main output file with the estimated network from each run
#less("net4.networks") # extra info

scores = [net0.loglik, net1.loglik, net2.loglik, net3.loglik, net4.loglik, net5.loglik] # The best is the (first) one with the lowest value
hmax = collect(0:5)
R"name = function(x) file.path(x)" # function to create file name in appropriate folder
R"svg(name('network_score.svg'), width=6, height=6)" # starts image file
R"par"(mar=[4,4,4,4]) # to reduce margins 
R"plot"(hmax, scores, type="b", ylab="network score", xlab="hmax", col="blue");
R"dev.off()"; # wrap up and save image file

net2t = snaq!(net0, raxmlCF, hmax=2, filename="net2t", seed=3456, runs=100)
rootatnode!(net2t, "icar")
R"name = function(x) file.path(x)" # function to create file name in appropriate folder
R"svg(name('net2t.svg'), width=8, height=12)" # starts image file
R"par"(mar=[0,0,0,0]) # to reduce margins (no margins at all here)
plot(net2t, :R, showGamma=true, showEdgeNumber=true); # network is plotted & sent to file
R"dev.off()"; # wrap up and save image file
net3t = snaq!(net0, raxmlCF, hmax=3, filename="net3t", seed=4567, runs=100)
rootatnode!(net3t, "icar")
R"name = function(x) file.path(x)" # function to create file name in appropriate folder
R"svg(name('net3t.svg'), width=8, height=12)" # starts image file
R"par"(mar=[0,0,0,0]) # to reduce margins (no margins at all here)
plot(net3t, :R, showGamma=true, showEdgeNumber=true); # network is plotted & sent to file
R"dev.off()"; # wrap up and save image file
#less("net3t.err") # would provide info about errors, if any
#less("net3t.out") # main output file with the estimated network from each run
#less("net3t.networks") # extra info
net4t = snaq!(net0, raxmlCF, hmax=4, filename="net4t", seed=5567, runs=100)
rootatnode!(net4t, "icar")
R"name = function(x) file.path(x)" # function to create file name in appropriate folder
R"svg(name('net4t.svg'), width=8, height=12)" # starts image file
R"par"(mar=[0,0,0,0]) # to reduce margins (no margins at all here)
plot(net4t, :R, showGamma=true, showEdgeNumber=true); # network is plotted & sent to file
R"dev.off()"; # wrap up and save image file
#less("net4t.err") # would provide info about errors, if any
#less("net4t.out") # main output file with the estimated network from each run
#less("net4t.networks") # extra info
net5t = snaq!(net0, raxmlCF, hmax=5, filename="net5t", seed=5567, runs=100)
rootatnode!(net5t, "icar")
R"name = function(x) file.path(x)" # function to create file name in appropriate folder
R"svg(name('net5t.svg'), width=8, height=12)" # starts image file
R"par"(mar=[0,0,0,0]) # to reduce margins (no margins at all here)
plot(net5t, :R, showGamma=true, showEdgeNumber=true); # network is plotted & sent to file
R"dev.off()";
scores = [net0.loglik, net1.loglik, net2t.loglik, net3t.loglik, net4t.loglik, net5t.loglik]
hamx = collect(0:5)
R"name = function(x) file.path(x)" # function to create file name in appropriate folder
R"svg(name('network_scoret.svg'), width=6, height=6)" # starts image file
R"par"(mar=[4,4,4,4]) # to reduce margins 
R"plot"(hamx, scores, type="b", ylab="network score", xlab="hmax", col="blue");
R"dev.off()"; # wrap up and save image file

## plotting loglic without 0
scores = [net1.loglik, net2.loglik, net3.loglik, net4.loglik, net5.loglik] # The best is the (first) one with the lowest value
hmax = collect(1:5)
R"name = function(x) file.path(x)" # function to create file name in appropriate folder
R"svg(name('network_score_no0.svg'), width=6, height=6)" # starts image file
R"par"(mar=[4,4,4,4]) # to reduce margins 
R"plot"(hmax, scores, type="b", ylab="network score", xlab="hmax", col="blue");
R"dev.off()"; # wrap up and save image file


scores = [net1.loglik, net2t.loglik, net3t.loglik, net4t.loglik, net5t.loglik]
hamx = collect(1:5)
R"name = function(x) file.path(x)" # function to create file name in appropriate folder
R"svg(name('network_scoret_no0.svg'), width=6, height=6)" # starts image file
R"par"(mar=[4,4,4,4]) # to reduce margins 
R"plot"(hamx, scores, type="b", ylab="network score", xlab="hmax", col="blue");
R"dev.off()"; # wrap up and save image file



## BELOW IS THE EXAMPLE! CHANGE TO YOUR NEEDS IF NEEDED!!!
## Here the best network is within net1. Can this network be optimize? Let's do it:


truenet = readTopology("((glandKZ,(aqui,((pyre,dard):2.028139255638847)#H10:0.859094841742487::0.7927476772959192)1:0.06884309861508807)0.71:0.2857301273573,gland,(zull,#H10:8.643961378157723::0.20725232270408078):0.4419475636930522);"); # Tree extracted from net1.networks

net1alt = topologyMaxQPseudolik!(truenet, raxmlCF);

net1alt.loglik

# 0.9831882689607946, same as net1.loglik, so no need for optimization


# Plot the distinct networks contained in net1.networks

file = "net1.networks"

netlist = readMultiTopology(file)


rootonedge!(netlist[1], 6)
R"name = function(x) file.path(x)" # function to create file name in appropriate folder
R"svg(name('bestn1.svg'), width=8, height=12)" # starts image file
R"par"(mar=[0,0,0,0]) # to reduce margins (no margins at all here)
plot(netlist[1], :R, showGamma=true, tipOffset=0.1, showEdgeNumber=true);
R"dev.off()"; # wrap up and save image file


rootonedge!(netlist[2], 5)
R"name = function(x) file.path(x)" # function to create file name in appropriate folder
R"svg(name('bestn12.svg'), width=8, height=12)" # starts image file
R"par"(mar=[0,0,0,0]) # to reduce margins (no margins at all here)
plot(netlist[2], :R, showGamma=true, tipOffset=0.1, showEdgeNumber=true) # Second best network
R"dev.off()"; # wrap up and save image file





